<!DOCTYPE html>
<html lang="{{lang}}">
  <head>
    {{> standard_headers}}
    <link type="text/css" rel="stylesheet" href="content-layout.css" />
  </head>
  <body>
    <main data-testid="app-express">
      {{{body}}}
      {{> scripts }}
    </main>
    <style>
      div:has( > [class*="ory_elements__button"]) {
        align-self: center;
      }
      [class*="ory_elements__typography_size_headline37"] {
        font-weight: bold;
        font-size: 1.5rem;
      }
      [class*="ory_elements__typography_size_headline26"] {
        font-size: 1.25rem;
      }
    </style>
  </body>
</html>
